'use client'

import styled from "styled-components";

const HeaderBox = styled.header`
  display: flex;
`

export default function Header() {
    return (
      <HeaderBox>
        헤더
      </HeaderBox>
    );
  }
  